import 'package:flutter/material.dart';

class EmergencyDepartment extends StatelessWidget {
  const EmergencyDepartment({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('EMERGENCY DEPARTMENT'),
      ),
    );
  }
}
